<%= @header %>

" Turn on syntax highlighting
syntax on

" Jump to last position when reopening a file
if has("autocmd")
  au BufReadPost * if line("'\"") > 1 && line("'\"") <= line("$") | exe "normal! g'\"" | endif
endif

" Load indentation rules and plugins according to the detected filetype.
if has("autocmd")
  filetype plugin indent on
endif

set showcmd		" Show (partial) command in status line.
set showmatch		" Show matching brackets.
set ignorecase		" Do case insensitive matching
set smartcase		" Do smart case matching
"set incsearch		" Incremental search
"set autowrite		" Automatically save before commands like :next and :make
"set hidden             " Hide buffers when they are abandoned
"set mouse=a		" Enable mouse usage (all modes)

set number " Display line number
set ls=2 " Display filename

set autoindent " Automatically detect indentation level

" Indent with hard tabs
set shiftwidth=8
set tabstop=8

" When doing tab completion, give the following files lower priority. You may
" wish to set 'wildignore' to completely ignore files, and 'wildmenu' to enable
" enhanced tab completion. These can be done in the user vimrc file.
set suffixes+=.info,.aux,.log,.dvi,.bbl,.out,.o,.lo,.d

" set all shell scripts to use bash highlighting
" 'sh' syntax and 'bash' syntax are slightly different
let g:is_bash = 1

" pathogen interface
if filereadable(expand("~/.vim/autoload/pathogen.vim"))
  execute pathogen#infect()
endif

" Set custom colors
hi Search ctermbg=None ctermfg=Grey
hi Comment ctermbg=None ctermfg=LightBlue

" python settings
au BufNewFile,BufRead *.py set tabstop=4 softtabstop=4 shiftwidth=4 expandtab smarttab autoindent

" rust settings
au BufNewFile,BufRead *.rs set tabstop=4 softtabstop=4 shiftwidth=4 expandtab smarttab autoindent

" ruby settings
au BufNewFile,BufRead *.rb set tabstop=2 softtabstop=2 shiftwidth=2 expandtab smarttab autoindent

" syntax highlighting for json
autocmd BufNewFile,BufRead *.json set filetype=javascript

" Disable Ex mode hotkey
nnoremap Q <Nop>

" vim: syntax=vim
